{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --prefix backend && npx prisma generate --schema=prisma/schema.prisma && npm run build --prefix backend"
  },
  "deploy": {
    "startCommand": "npm run start --prefix backend",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
